import { StyleSheet } from 'react-native'
import { Size } from '../../constants'

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    zIndex: 2,
    top: Size.large - 10,
  },
})
